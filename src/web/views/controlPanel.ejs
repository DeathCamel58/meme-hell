<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <!-- Include Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
<div class="container mt-5">
    <h1 class="mb-4"><%= title %></h1>

    <div class="row">
        <h2>User Renaming</h2>
        <div class="row">
            <div class="col" style="max-height: 250px; overflow: auto;">
                <h3>Adjectives</h3>
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <tbody>
                        <% for (let i = 0; i < botConfig.adjectives.length; i++) { %>
                            <tr>
                                <td><%= botConfig.adjectives[i] %></td>
                            </tr>
                        <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="col" style="max-height: 250px; overflow: auto;">
                <h3>Nouns</h3>
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <tbody>
                        <% for (let i = 0; i < botConfig.nouns.length; i++) { %>
                            <tr>
                                <td><%= botConfig.nouns[i] %></td>
                            </tr>
                        <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <form action="/setRenameAll" method="POST">
            <div class="form-check mb-3">
                <input type="checkbox" class="form-check-input" id="setRenameAll" name="setRenameAll" <% if (botConfig.enableRenameAll) { %>checked<% } %>>
                <label class="form-check-label" for="setRenameAll">Enable Auto RenameAll</label>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>

    <hr />

    <div class="row">
        <h2>Role Juggling</h2>

        <form action="/setRoleJuggle" method="POST">
            <div class="form-check mb-3">
                <input type="checkbox" class="form-check-input" id="setRoleJuggle" name="setRoleJuggle" <% if (botConfig.enableRoleJuggler) { %>checked<% } %>>
                <label class="form-check-label" for="setRoleJuggle">Enable Role Juggling</label>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>

    <hr />

    <div class="row">
        <h2>Channel Juggling</h2>

        <form action="/setChannelJuggle" method="POST">
            <div class="form-check mb-3">
                <input type="checkbox" class="form-check-input" id="setChannelJuggle" name="setChannelJuggle" <% if (botConfig.enableChannelJuggler) { %>checked<% } %>>
                <label class="form-check-label" for="setChannelJuggle">Enable Channel Juggling</label>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
        </form>
    </div>
</div>

<!-- Include Bootstrap JS and Popper.js (required for Bootstrap JS) -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>
</body>
</html>
